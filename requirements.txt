# KaraokeTool - Python 3.12
# Kern (nu al gebruikt)
numpy>=1.26
soundfile>=0.12

# Spraakherkenning (vanaf versie 0.2)
faster-whisper>=1.0

# Taaldetectie op de songtekst
langdetect>=1.0

# Fonetische clustering (snelle stringafstanden)
rapidfuzz>=3.0

# Uitlijning: MFCC, chroma, onsets, cross-correlatie (vanaf versie 0.4)
librosa>=0.10
scipy>=1.11

# Lettergreepmodel (Nederlandse woordafbreking voor de timing; B150)
pyphen>=0.14

# Karaokevideo-render
pillow>=10.0

# Grafische interface
PySide6>=6.6

# Ontwikkeling en tests
pytest>=8.0

# Optioneel (slimme modellen; groot). Installeer handmatig als gewenst:
#   pip install demucs        (zang scheiden / karaoke maken)
#   pip install whisperx      (preciezere woordtijden)
