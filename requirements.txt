# KaraokeTool - Python 3.12
# Core (already in use)
numpy>=1.26
soundfile>=0.12

# Speech recognition (from version 0.2)
faster-whisper>=1.0

# Language detection on the lyrics
langdetect>=1.0

# Phonetic clustering (fast string distances)
rapidfuzz>=3.0

# Alignment: MFCC, chroma, onsets, cross-correlation (from version 0.4)
librosa>=0.10
scipy>=1.11

# Syllable model (Dutch hyphenation for the timing; B150)
pyphen>=0.14

# Karaoke video render
pillow>=10.0

# Graphical interface
PySide6>=6.6

# Development and tests
pytest>=8.0

# Optional (the large models; big). Install by hand if wanted:
#   pip install demucs        (separate the vocals / make karaoke)
#   pip install whisperx      (more precise word times)
